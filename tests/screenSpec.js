describe('Testing Screen', function() {
  it('Should return a new instance of Screen');
  it('Should create an element inside the $el index of the instance');
  it('Should render the given number inside the calculator whe using the render method');
});
